# tasks

Task queue for agents. Simple question/answer type with statuses. Agents create pending tasks; humans or Claude answer or reject them through typed actions.

## Types
- `task` — `question`, `answer`, `status`; actions: `respond`, `reject`
