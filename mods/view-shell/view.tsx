// Placeholder. Step 8 replaces this with the migrated ViewPage body.

import { view } from '@treenx/react';
import { ViewShell } from './types';

const ViewShellView = () => (
  <div className="p-4 text-sm italic text-[--text-3]">
    view-shell placeholder — ViewPage body migration pending
  </div>
);

view(ViewShell, ViewShellView);
