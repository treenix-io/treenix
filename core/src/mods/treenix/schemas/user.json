{
  "type": "object",
  "title": "Account lifecycle state for auth and access decisions.",
  "properties": {
    "status": {
      "type": "string",
      "enum": [
        "active",
        "pending",
        "blocked"
      ],
      "title": "Status",
      "default": "pending"
    }
  },
  "required": [
    "status"
  ],
  "$id": "user",
  "$schema": "http://json-schema.org/draft-07/schema#"
}
