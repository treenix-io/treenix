{
  "private": true,
  "type": "module",
  "workspaces": [
    "core",
    "packages/*",
    "mods/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/treenix-ai/treenix.git"
  },
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "test:e2e": "playwright test",
    "dev:server": "npm run dev:server -w @treenx/core",
    "dev:front": "npm run dev -w @treenx/react",
    "schema": "npm run schema -w @treenx/core",
    "typecheck": "tsc -p core --noEmit && tsc -p packages/react --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/d3-selection": "^3.0.11",
    "@types/d3-transition": "^3.0.9",
    "@types/d3-zoom": "^3.0.8",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "onnxruntime-node": "1.24.2"
  },
  "dependencies": {
    "d3-selection": "^3.0.0",
    "d3-zoom": "^3.0.0"
  }
}
